/**
 * Canonical path inside a sandbox where per-org/artist workspace files live.
 * Shared by the `/files` page filetree and the `@`-mention file picker so both
 * stay in lockstep if the sandbox layout changes.
 */
export const WORKSPACE_ORGS_PATH = ".openclaw/workspace/orgs";
