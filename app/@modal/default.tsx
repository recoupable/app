/**
 * Nothing to show in the modal slot on a normal navigation.
 *
 * Required by parallel routes: without a default, a hard load of any route
 * that has not filled this slot 404s.
 */
const Default = () => null;

export default Default;
